% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hy_classes.R
\name{hy_flownetwork}
\alias{hy_flownetwork}
\title{hy_flownetwork: non-dendritic junction table}
\description{
A \code{hy_flownetwork} object is a junction table — a non-dendritic edge
list of \code{id} and \code{toid} rows where \code{id} may repeat. Each row records
one connection between an entity (a catchment or flowline) and a
downstream entity, and optional \code{upmain}/\code{downmain} logical columns
annotate which of the connections at a divergence is the main path.
This is the only edge-list-shaped representation in hydroloom that
preserves divergences directly.

Unlike the other class pages on this index, \code{hy_flownetwork} does
\strong{not} inherit from \code{hy}. It is a separate junction table — it does
not carry the \code{orig_names} attribute and it does not pass \code{\link[=is.hy]{is.hy()}}.
}
\details{
\code{hy_flownetwork} is what hydroloom's internal classifier produces
(via \code{\link[=hy]{hy()}}) when an \code{id}/\code{toid} table has duplicated \code{id} values,
and what \code{\link[=to_flownetwork]{to_flownetwork()}} produces from a \code{hy_leveled} input. The \code{upmain}/\code{downmain} annotation, when
present, lets divergence-aware operations choose a main path without
having to re-derive it from levelpath identity each time.

Several hydroloom operations that were originally written against
\code{hy_topo} accept \code{hy_flownetwork} directly through delegate methods,
because the underlying algorithm already tolerates non-dendritic
input: \code{\link[=add_streamorder]{add_streamorder()}}, \code{\link[=accumulate_downstream]{accumulate_downstream()}},
\code{\link[=get_bridge_flowlines]{get_bridge_flowlines()}}, \code{\link[=add_levelpaths]{add_levelpaths()}}, and
\code{\link[=make_node_topology]{make_node_topology()}} all work on \code{hy_flownetwork} without
conversion.
}
\section{Required columns}{

\itemize{
\item \code{id} — entity identifier; may be non-unique
\item \code{toid} — downstream entity identifier
}

Optional:
\itemize{
\item \code{upmain} — logical, \code{TRUE} for the main upstream connection
at each junction
\item \code{downmain} — logical, \code{TRUE} for the main downstream
connection at each junction
}

See \link{hydroloom_name_definitions} for the canonical column definitions.
}

\section{Functions that operate on hy_flownetwork}{

\itemize{
\item Native operations: \code{\link[=navigate_network_dfs]{navigate_network_dfs()}}, \code{\link[=make_index_ids]{make_index_ids()}}
\item Delegated to the \code{hy_topo} algorithm: \code{\link[=add_streamorder]{add_streamorder()}},
\code{\link[=accumulate_downstream]{accumulate_downstream()}}, \code{\link[=get_bridge_flowlines]{get_bridge_flowlines()}},
\code{\link[=add_levelpaths]{add_levelpaths()}}, \code{\link[=make_node_topology]{make_node_topology()}}
}

Call \code{\link[=hy_capabilities]{hy_capabilities()}} on a specific object for the authoritative
list given its current columns.
}

\section{Conversions to other representations}{

\itemize{
\item To \link{hy_node} (bipartite graph): \code{\link[=make_node_topology]{make_node_topology()}}
}

Going the other direction — from \code{hy_node} or \code{hy_leveled} into
\code{hy_flownetwork} — is what \code{\link[=to_flownetwork]{to_flownetwork()}} is for.
}

\examples{
x <- sf::read_sf(system.file("extdata/new_hope.gpkg", package = "hydroloom"))

z <- to_flownetwork(x)

hy_network_type(z)

z

}
\seealso{
\link{hy}, \link{hy_topo}, \link{hy_leveled}, \link{hy_node},
\code{\link[=hy_capabilities]{hy_capabilities()}}, \code{\link[=hy_network_type]{hy_network_type()}}, \code{\link[=is_dendritic]{is_dendritic()}},
\code{\link[=to_flownetwork]{to_flownetwork()}}, \code{\link[=navigate_network_dfs]{navigate_network_dfs()}}, \code{\link[=make_index_ids]{make_index_ids()}}
}
