% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/decompose_network.R
\name{get_domain}
\alias{get_domain}
\title{Get a domain by id}
\usage{
get_domain(decomposition, domain_id)
}
\arguments{
\item{decomposition}{object of class \code{domain_decomposition}.}

\item{domain_id}{character(1). Domain id to look up.}
}
\value{
object of class \code{hy_domain} — the named list with all six
slots described in \code{\link[=hy_domain]{hy_domain()}}.
}
\description{
Returns the \code{\link[=hy_domain]{hy_domain()}} object stored under \code{domain_id} in a
decomposition. Errors when \code{domain_id} is not a key of
\code{decomposition$domains}.
}
\examples{
g <- sf::read_sf(system.file("extdata/walker.gpkg", package = "hydroloom"))

h <- hy(g) |>
  add_toids() |>
  add_levelpaths(name_attribute = "GNIS_ID",
    weight_attribute = "arbolate_sum")

d <- decompose_network(h)

get_domain(d, names(d$domains)[[1]])

}
\seealso{
\code{\link[=hy_domain]{hy_domain()}} for the per-domain object,
\code{\link[=get_domain_connectivity]{get_domain_connectivity()}}, \code{\link[=get_domain_for_catchment]{get_domain_for_catchment()}},
\code{\link[=get_domain_graph]{get_domain_graph()}}.
}
