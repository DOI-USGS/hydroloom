% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hy_classes.R
\name{hy_node}
\alias{hy_node}
\title{hy_node: bipartite catchment/flowline-and-nexus graph}
\description{
A \code{hy_node} object is a bipartite graph: each entity (a catchment or
flowline) is one row, carrying a \code{fromnode} (the upstream nexus it
leaves) and a \code{tonode} (the downstream nexus it enters). Connectivity
is recovered by matching \code{tonode} to \code{fromnode} across rows. This is
the representation that natively supports divergences without
duplicating rows — a divergence is simply a nexus with more than one
outgoing entity.

\code{hy_node} inherits from \code{hy}.
}
\details{
Because divergences are encoded through shared node identifiers
rather than row duplication, \code{hy_node} requires unique \code{id} even on
non-dendritic networks. The \code{fromnode}/\code{tonode} pair carries the
connectivity that an \code{id}/\code{toid} edge list cannot.

\code{hy_node} is the entry point for hydroloom's divergence-aware
operations: \code{\link[=add_divergence]{add_divergence()}} flags main and diverted paths from
geometry, \code{\link[=add_return_divergence]{add_return_divergence()}} marks where diverted paths return
to the main, and \code{\link[=subset_network]{subset_network()}} walks the bipartite graph during
subsetting.
}
\section{Required columns}{

\itemize{
\item \code{id} — entity identifier, unique across rows
\item \code{fromnode} — upstream nexus identifier
\item \code{tonode} — downstream nexus identifier
}

See \link{hydroloom_name_definitions} for the canonical column definitions.
}

\section{Functions that operate on hy_node}{

\itemize{
\item Divergence handling: \code{\link[=add_divergence]{add_divergence()}}, \code{\link[=add_return_divergence]{add_return_divergence()}}
\item Subsetting: \code{\link[=subset_network]{subset_network()}}
\item Edge-list construction: \code{\link[=add_toids]{add_toids()}}
}

Call \code{\link[=hy_capabilities]{hy_capabilities()}} on a specific object for the authoritative
list given its current columns.
}

\section{Conversions to other representations}{

\itemize{
\item To \link{hy_topo} (self-referencing edge list): \code{\link[=add_toids]{add_toids()}}.
With \code{return_dendritic = TRUE} (the default) the resulting \code{toid}
drops secondary paths at divergences; set the option to \code{FALSE}
and route through \code{\link[=to_flownetwork]{to_flownetwork()}} to keep them.
}
}

\examples{
x <- sf::read_sf(system.file("extdata/new_hope.gpkg", package = "hydroloom"))

z <- hy(x)

hy_network_type(z)

z

}
\seealso{
\link{hy}, \link{hy_topo}, \link{hy_leveled}, \link{hy_flownetwork},
\code{\link[=hy_capabilities]{hy_capabilities()}}, \code{\link[=hy_network_type]{hy_network_type()}},
\code{\link[=make_node_topology]{make_node_topology()}}, \code{\link[=add_toids]{add_toids()}}, \code{\link[=add_divergence]{add_divergence()}},
\code{\link[=subset_network]{subset_network()}}
}
