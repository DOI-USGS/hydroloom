% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/decompose_network.R
\name{print.domain_decomposition}
\alias{print.domain_decomposition}
\title{Print a domain_decomposition}
\usage{
\method{print}{domain_decomposition}(x, full = FALSE, ..., width = getOption("width"))
}
\arguments{
\item{x}{object of class \code{domain_decomposition}.}

\item{full}{logical. \code{FALSE} (default) prints the cheap summary;
\code{TRUE} prints the full hierarchical tree.}

\item{...}{ignored.}

\item{width}{integer. Reserved for future use; currently ignored.}
}
\value{
\code{x}, invisibly.
}
\description{
Two-mode S3 print method for \code{domain_decomposition}. The default
(cheap) form prints a fixed-size summary of slot counts: trunks,
compacts, catchments, edges, nexuses, and overrides. Cost is bounded
by the number of domains, so it stays fast on 50,000-catchment
decompositions. The full form prints a hierarchical tree summary
with per-block roll-up statistics and is intended for verifying the
shape of a freshly-built decomposition before running analysis on it.
}
\details{
The cheap form is the default because at the REPL most users want a
quick "did this come out the way I expected" check, not a full audit.
The footer line of the cheap form advertises how to call the full
form.

The \code{width} argument is reserved for future column-wrapping support
and is currently ignored by both modes.
}
