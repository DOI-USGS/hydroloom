% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/decompose_network.R
\name{is_root_domain}
\alias{is_root_domain}
\title{Test whether a domain is the root of its basin}
\usage{
is_root_domain(decomposition, domain_id)
}
\arguments{
\item{decomposition}{object of class \code{domain_decomposition}.}

\item{domain_id}{character(1). Domain id to test.}
}
\value{
logical(1).
}
\description{
A domain is the \emph{root} when its outlet nexus has no downstream
domain — the registry row for its \code{outlet_nexus_id} carries
\code{to_domain_id = NA}. The root is the basin's most-downstream domain.
}
\examples{
g <- sf::read_sf(system.file("extdata/walker.gpkg", package = "hydroloom"))

h <- hy(g) |>
  add_toids() |>
  add_levelpaths(name_attribute = "GNIS_ID",
    weight_attribute = "arbolate_sum")

d <- decompose_network(h)

is_root_domain(d, names(d$domains)[[1]])

}
\seealso{
\code{\link[=is_leaf_domain]{is_leaf_domain()}}, \code{\link[=is_stem_domain]{is_stem_domain()}}, \code{\link[=hy_domain]{hy_domain()}}.
}
