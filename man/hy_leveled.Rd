% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hy_classes.R
\name{hy_leveled}
\alias{hy_leveled}
\title{hy_leveled: enriched self-referencing edge list}
\description{
A \code{hy_leveled} object is a \code{hy_topo} carrying the additional columns
produced by stream leveling: \code{topo_sort}, \code{levelpath}, and
\code{levelpath_outlet_id}. These columns are what mainstem-aware operations
(Pfafstetter coding, stream level, junction-table conversion) need in
order to run.

\code{hy_leveled} is a strict subclass of \code{hy_topo}, so every function that
accepts a \code{hy_topo} accepts a \code{hy_leveled} as well.
}
\details{
\code{hy_leveled} exists to mark a \code{hy_topo} as having been through
\code{\link[=add_levelpaths]{add_levelpaths()}} so downstream functions can dispatch on the presence
of leveling without re-checking column names. The leveling columns
encode mainstem path identity (\code{levelpath}), the outlet that closes
that path (\code{levelpath_outlet_id}), and the topological order along
the network (\code{topo_sort}).

Like \code{hy_topo}, \code{hy_leveled} requires unique \code{id} and cannot represent
divergences as duplicated rows. Convert to \link{hy_flownetwork} via
\code{\link[=to_flownetwork]{to_flownetwork()}} to preserve main and diverted paths in junction-table
form.
}
\section{Required columns}{

\itemize{
\item \code{id} — entity identifier, unique across rows
\item \code{toid} — downstream entity identifier
\item \code{topo_sort} — topological sort order (NHDPlus hydrosequence)
\item \code{levelpath} — mainstem path identifier
\item \code{levelpath_outlet_id} — outlet \code{id} that closes each levelpath
}

See \link{hydroloom_name_definitions} for the canonical column definitions.
}

\section{Functions that operate on hy_leveled}{

All \code{hy_topo} methods, plus the leveling-aware operations:
\itemize{
\item Mainstem coding: \code{\link[=add_pfafstetter]{add_pfafstetter()}}, \code{\link[=add_streamlevel]{add_streamlevel()}}
\item Junction-table conversion: \code{\link[=to_flownetwork]{to_flownetwork()}}
}

Call \code{\link[=hy_capabilities]{hy_capabilities()}} on a specific object for the authoritative
list given its current columns.
}

\section{Conversions to other representations}{

\itemize{
\item To \link{hy_node} (bipartite graph): \code{\link[=make_node_topology]{make_node_topology()}}
\item To \link{hy_flownetwork} (junction table): \code{\link[=to_flownetwork]{to_flownetwork()}},
which uses \code{levelpath} to decide which connection at a divergence
is the main path
}
}

\examples{
x <- sf::read_sf(system.file("extdata/new_hope.gpkg", package = "hydroloom"))

z <- add_levelpaths(add_toids(hy(x)),
                    name_attribute = "GNIS_ID",
                    weight_attribute = "arbolate_sum")

hy_network_type(z)

}
\seealso{
\link{hy}, \link{hy_topo}, \link{hy_node}, \link{hy_flownetwork},
\code{\link[=hy_capabilities]{hy_capabilities()}}, \code{\link[=hy_network_type]{hy_network_type()}},
\code{\link[=add_levelpaths]{add_levelpaths()}}, \code{\link[=add_pfafstetter]{add_pfafstetter()}}, \code{\link[=to_flownetwork]{to_flownetwork()}}
}
