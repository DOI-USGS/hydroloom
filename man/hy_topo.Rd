% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hy_classes.R
\name{hy_topo}
\alias{hy_topo}
\title{hy_topo: self-referencing edge list}
\description{
A \code{hy_topo} object is a self-referencing edge list: one row per
entity (a catchment or flowline), carrying an \code{id} and a \code{toid} that
points at the immediately downstream entity. This is the main
representation for downstream traversal, topological sorting, and
most accumulation algorithms in hydroloom.

\code{hy_topo} inherits from \code{hy}. The enriched subclass \code{hy_leveled} extends
\code{hy_topo} with topo-sort and levelpath columns, so methods written for
\code{hy_topo} apply to \code{hy_leveled} objects without modification.
}
\details{
\code{hy_topo} requires unique \code{id}. A divergence would need two downstream
connections from the same entity — that is, two rows with the same
\code{id} — and \code{hy_topo} does not permit that. Networks with divergences
belong in \link{hy_node} (a bipartite graph through \code{fromnode}/\code{tonode}) or
in \link{hy_flownetwork} (a junction table that annotates main and diverted
paths). When the internal classifier called from \code{\link[=hy]{hy()}} sees a duplicated
\code{id} in an \code{id}/\code{toid} table, it produces an \code{hy_flownetwork} rather than
an \code{hy_topo}.

For the authoritative, programmatic view of which functions are callable
on a particular object, use \code{\link[=hy_capabilities]{hy_capabilities()}}.
}
\section{Required columns}{

\itemize{
\item \code{id} — entity identifier, unique across rows
\item \code{toid} — identifier of the immediately downstream entity;
network outlets carry a sentinel value
}

See \link{hydroloom_name_definitions} for the canonical column definitions
and accepted aliases.
}

\section{Functions that operate on hy_topo}{

\itemize{
\item Topology and sorting: \code{\link[=sort_network]{sort_network()}}, \code{\link[=add_topo_sort]{add_topo_sort()}},
\code{\link[=check_hy_graph]{check_hy_graph()}}
\item Path enrichment: \code{\link[=add_levelpaths]{add_levelpaths()}}, \code{\link[=add_pathlength]{add_pathlength()}},
\code{\link[=add_streamorder]{add_streamorder()}}
\item Accumulation and traversal: \code{\link[=accumulate_downstream]{accumulate_downstream()}},
\code{\link[=navigate_hydro_network]{navigate_hydro_network()}}, \code{\link[=navigate_network_dfs]{navigate_network_dfs()}},
\code{\link[=make_index_ids]{make_index_ids()}}
}

Call \code{\link[=hy_capabilities]{hy_capabilities()}} on a specific object to see which functions
are callable on it given its current columns.
}

\section{Conversions to other representations}{

\itemize{
\item To \link{hy_node} (bipartite graph): \code{\link[=make_node_topology]{make_node_topology()}}
\item To \link{hy_leveled} (enriched edge list): \code{\link[=add_levelpaths]{add_levelpaths()}},
which adds \code{topo_sort}, \code{levelpath}, and \code{levelpath_outlet_id}
\item To \link{hy_flownetwork} (junction table): \code{\link[=add_levelpaths]{add_levelpaths()}} then
\code{\link[=to_flownetwork]{to_flownetwork()}}
}
}

\examples{
x <- sf::read_sf(system.file("extdata/new_hope.gpkg", package = "hydroloom"))

z <- add_toids(hy(x))

hy_network_type(z)

z

}
\seealso{
\link{hy}, \link{hy_leveled}, \link{hy_node}, \link{hy_flownetwork},
\code{\link[=hy_capabilities]{hy_capabilities()}}, \code{\link[=hy_network_type]{hy_network_type()}}, \code{\link[=is_dendritic]{is_dendritic()}},
\code{\link[=add_toids]{add_toids()}}, \code{\link[=sort_network]{sort_network()}}, \code{\link[=make_node_topology]{make_node_topology()}}
}
