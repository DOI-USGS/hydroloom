% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/check_valid.R
\name{check_valid}
\alias{check_valid}
\title{Check and Repair Geometry Validity}
\usage{
check_valid(x, out_prj = sf::st_crs(x))
}
\arguments{
\item{x}{sf data.frame or NULL}

\item{out_prj}{crs. Target CRS for the output, passed to
\code{\link[sf]{st_transform}}. Defaults to the CRS of \code{x}.}
}
\value{
sf data.frame with valid geometry, or NULL if \code{x} is NULL.
}
\description{
Validates sf geometry, repairs invalid features, unifies
geometry types, and optionally reprojects. Handles GEOMETRYCOLLECTION
artifacts from \code{st_make_valid()} by extracting the dominant
geometry type.
}
\examples{
library(sf)
p1 <- st_polygon(list(
  rbind(c(0, 0), c(10, 0), c(10, 10), c(0, 10), c(0, 0)),
  rbind(c(2, 2), c(2, 8), c(8, 8), c(8, 2), c(2, 2))
))
x <- st_sf(geometry = st_sfc(p1, crs = 5070))
result <- check_valid(x)

}
