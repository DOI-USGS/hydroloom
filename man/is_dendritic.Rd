% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hy_classes.R
\name{is_dendritic}
\alias{is_dendritic}
\title{Is the network dendritic?}
\usage{
is_dendritic(x, validate_data = TRUE)
}
\arguments{
\item{x}{data.frame or hy object to test.}

\item{validate_data}{logical. If \code{FALSE}, only checks the
\code{divergence} column (fast). If \code{TRUE} (default), also
checks for duplicated ids.}
}
\value{
logical. TRUE if the network is dendritic.
}
\description{
Checks whether the network contains any divergences.
Returns \code{FALSE} if a \code{divergence} column exists with any
value of 2, or if the table has a \code{toid} column and duplicated
\code{id} values (indicating row duplication from divergences).
Otherwise returns \code{TRUE}.
}
\examples{
x <- sf::read_sf(system.file("extdata/new_hope.gpkg", package = "hydroloom"))
is_dendritic(hy(x))

}
