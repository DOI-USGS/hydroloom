% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/decompose_network.R
\name{decompose_network}
\alias{decompose_network}
\title{Decompose a network into domains}
\usage{
decompose_network(
  x,
  stem_metric = "drainage_area",
  stem_threshold = NULL,
  stem_levelpaths = NULL,
  domain_breaks = NULL,
  overrides = NULL
)
}
\arguments{
\item{x}{\code{hy_leveled} object (dendritic network already enriched
with levelpaths).}

\item{stem_metric}{character. Metric evaluated at each levelpath
outlet to decide stem eligibility. \code{"drainage_area"} reads
\code{total_da_sqkm}; \code{"arbolate_sum"} reads \code{arbolate_sum}. Only
consulted when \code{stem_threshold} is non-NULL.}

\item{stem_threshold}{numeric scalar or \code{NULL}. Value of
\code{stem_metric} at a levelpath outlet above which the levelpath is
a stem candidate. \code{NULL} (default) falls back to one stem per
drainage basin (the basin's outlet levelpath). When non-NULL,
the input must carry a \code{stream_calculator} column; call
\code{\link[=add_streamorder]{add_streamorder()}} to add it if the source data does not
already provide one (NHDPlus \code{StreamCalc}, canonicalized by
\code{\link[=hy]{hy()}}, counts).}

\item{stem_levelpaths}{vector of levelpath ids or \code{NULL}. When
non-NULL, bypasses the threshold rule and forces these levelpaths
to be stems (the basin's terminal-outlet levelpath is always unioned
in). Every id must exist in \code{x$levelpath}.}

\item{domain_breaks}{vector of catchment ids or \code{NULL}. When
non-NULL, these stem catchment ids define where the stem network
is segmented into domain groups. Each break id becomes a
segment terminal in addition to the auto-detected confluences and
outlets. Breaks that are not stem catchments in a given basin
are silently ignored. When \code{NULL} (default), segmentation is
determined automatically from stem confluences and (if available)
bridge flowlines.}

\item{overrides}{data.frame. Non-dendritic inter-domain transfer
table; pass-through to \code{decomposition$overrides}.}
}
\value{
a \link{domain_decomposition} object.
}
\description{
Partitions a hydrologic network into \code{hy_domain} objects for
independent or parallel computation. Each drainage basin is split
into domains along its extensive network; the extensive network
itself is returned separately as the basin's \emph{domain_connectivity}
overlay. See \code{\link[=hy_domain]{hy_domain()}} and \link{domain_decomposition} for details.
}
\details{
\strong{Input.} Input must be \code{hy_leveled} -- the network must already
carry \code{levelpath}, \code{levelpath_outlet_id}, and \code{topo_sort} columns.
Call \code{\link[=add_levelpaths]{add_levelpaths()}} to add. Non-dendritic sources
(\code{hy_flownetwork}) are not supported at this time.

\strong{Extensive network selection.} Each drainage basin's extensive
network is selected from \code{stem_metric}, \code{stem_threshold}, and
\code{stem_levelpaths} (see arguments). The basin's extensive connectivity
(the regional relationship that ties its drainage pieces together) is
materialized as the basin's \code{domain_connectivity[[basin_id]]} overlay
— a \code{hy_leveled} view of the extensive network. The \verb{stem_*} naming
reflects the cross-scale view: a basin's outlet levelpath is a trunk,
but with a threshold or explicit override the selection can pull in
branches as well — every selected path is a \emph{stem} in the cross-scale
sense (trunks and branches are both stems).

\strong{Containment.} This function does not detect containment. A
drainage basin that the caller wants treated as belonging inside
another -- typically an endorheic basin or a drainage-divide
remnant -- is partitioned here as an independent basin like any
other. After decomposition, the caller declares the relationship
with \code{\link[=set_containment]{set_containment()}}; \code{\link[=recompose]{recompose()}} applies it when called
with \code{containment = "accumulate"}. The relationship is recorded on
the contained domain's \code{containing_domain_id} slot and surfaced by
\code{\link[=get_domain_graph]{get_domain_graph()}} with \code{relations = "contained"}. It does not
appear in \code{nexus_registry} because no flow crosses a hydro nexus
between the two basins.
}
\examples{
g <- sf::read_sf(system.file("extdata/walker.gpkg", package = "hydroloom"))

h <- hy(g) |>
  add_toids() |>
  add_levelpaths(name_attribute = "GNIS_ID",
    weight_attribute = "arbolate_sum")

d <- decompose_network(h)

length(d$domains)

}
\seealso{
\link{domain_decomposition} for the returned object's slots,
\code{\link[=hy_domain]{hy_domain()}} for the per-domain object, \code{\link[=validate_decomposition]{validate_decomposition()}},
\code{\link[=get_domain_graph]{get_domain_graph()}}, \code{\link[=get_domain_for_catchment]{get_domain_for_catchment()}}.
}
