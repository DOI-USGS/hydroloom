% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/decompose_network.R
\name{get_domain_connectivity}
\alias{get_domain_connectivity}
\title{Get a basin's extensive network overlay}
\usage{
get_domain_connectivity(decomposition, basin_id = NULL)
}
\arguments{
\item{decomposition}{object of class \code{domain_decomposition}.}

\item{basin_id}{character(1) or \code{NULL}. Basin id to look up.
\code{NULL} (default) returns the full named list.}
}
\value{
single \code{hy_leveled} overlay when \code{basin_id} is supplied;
named list of overlays when \code{basin_id} is \code{NULL}.
}
\description{
Returns the \code{hy_leveled} overlay stored under \code{basin_id} in
\code{decomposition$domain_connectivity}, or — when \code{basin_id} is \code{NULL}
(the default) — the full named list of overlays. The overlay is the
basin's \emph{extensive network}: a \code{hy_leveled} view of the connecting
flowlines with \code{toid}s intact except at the basin outlet, which
carries the reserved outlet \code{toid} value.
}
\examples{
g <- sf::read_sf(system.file("extdata/walker.gpkg", package = "hydroloom"))

h <- hy(g) |>
  add_toids() |>
  add_levelpaths(name_attribute = "GNIS_ID",
    weight_attribute = "arbolate_sum")

d <- decompose_network(h)

get_domain_connectivity(d, names(d$domain_connectivity)[[1]])

}
\seealso{
\code{\link[=hy_domain]{hy_domain()}} for the dual-ownership rule that motivates
the overlay, \link{domain_decomposition}, \code{\link[=get_domain]{get_domain()}},
\code{\link[=get_domain_graph]{get_domain_graph()}}.
}
