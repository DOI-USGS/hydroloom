% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/decompose_network.R
\name{get_nexus_registry}
\alias{get_nexus_registry}
\title{Get the nexus registry from a decomposition}
\usage{
get_nexus_registry(decomposition)
}
\arguments{
\item{decomposition}{object of class \code{domain_decomposition}.}
}
\value{
data.frame.
}
\description{
Returns \code{decomposition$nexus_registry} -- the table that records each
synthetic hydro nexus produced by \code{\link[=decompose_network]{decompose_network()}} and which
domains it connects. One row per nexus; the columns are \code{nexus_id},
\code{from_domain_id}, and \code{to_domain_id} (NA at basin outlets where no
downstream domain receives the handoff).
}
\examples{
g <- sf::read_sf(system.file("extdata/walker.gpkg", package = "hydroloom"))

h <- hy(g) |>
  add_toids() |>
  add_levelpaths(name_attribute = "GNIS_ID",
    weight_attribute = "arbolate_sum")

d <- decompose_network(h)

head(get_nexus_registry(d))

}
\seealso{
\link{domain_decomposition}, \code{\link[=get_domain_graph]{get_domain_graph()}}.
}
